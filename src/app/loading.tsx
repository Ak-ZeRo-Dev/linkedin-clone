import LoadingSpinner from "@/components/LoadingSpinner";

const Loading = () => {
  return (
    <section className="grid h-svh w-full place-content-center">
      <LoadingSpinner />
    </section>
  );
};
export default Loading;
